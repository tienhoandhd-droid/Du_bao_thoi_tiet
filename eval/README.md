# Evaluation workspace

Owner: Validation. Chỉ lưu dataset tổng hợp/đã khử nhận dạng và report an toàn
cho repo public. Không commit tài liệu, câu hỏi, câu trả lời hoặc report GMP thật.

- `fixtures/`: dữ liệu giả để kiểm thử tooling.
- `golden-questions/`, `retrieval/`, `ragas/`, `promptfoo/`, `deepeval/`: scaffold
  cho các Chat eval sau.
- `reports/`: chỉ report đã redaction và được phê duyệt để lưu source.

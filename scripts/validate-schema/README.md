# Schema Validation Scripts

Các script tương lai kiểm schema coverage, migration/rollback pairing, RLS,
append-only grants và data contracts. SQL read-only hiện tại:
`supabase/tests/validate_master_schema.sql`.
